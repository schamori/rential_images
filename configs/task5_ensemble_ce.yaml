# Task 5 — Ensemble
# Train N models (different seeds) each with CE loss, no class weights, no resampling,
loss: "ce"
use_class_weights: false
sampler: "default"
ensemble: true
ensemble_n: 5
ensemble_seeds: [42, 7, 123, 363, 789]

